interface:
  display_name: "DIG Controllers"
  short_description: "Start DIG controllers in the running robot stack"
  default_prompt: "Use $dig-controllers to start or restart the requested DIG controller in tmux."

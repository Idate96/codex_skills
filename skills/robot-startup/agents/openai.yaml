interface:
  display_name: "Robot Startup"
  short_description: "Start robot prerequisites or Nav2 safely"
  default_prompt: "Use $robot-startup to start the real-robot prerequisites or add Nav2 safely to verified running components."

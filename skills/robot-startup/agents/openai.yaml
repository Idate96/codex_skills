interface:
  display_name: "Robot Startup"
  short_description: "Start the real-robot ROS prerequisites safely"
  default_prompt: "Use $robot-startup to start or restart the real-robot Moleworks ROS prerequisites and perform only explicitly authorized machine-ready actions."

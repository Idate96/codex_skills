interface:
  display_name: "Robot Startup"
  short_description: "Start the real-robot Moleworks ROS stack"
  default_prompt: "Use $robot-startup to start or restart the real-robot Moleworks ROS tmux stack with the requested controller and RPM setup."

interface:
  display_name: "Robot Move Check"
  short_description: "Run a bounded real-robot joint direction check"
  default_prompt: "Use $robot-move-check to run one recorded, guarded PID velocity step for the explicitly requested real-robot joint direction check."

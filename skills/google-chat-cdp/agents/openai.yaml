interface:
  display_name: "Google Chat CDP"
  short_description: "Use Google Chat through the live Chrome session"
  default_prompt: "Use $google-chat-cdp with $chrome-cdp to verify the active Google Chat account, open a DM or space, and send or inspect messages in the user's existing Chrome session."

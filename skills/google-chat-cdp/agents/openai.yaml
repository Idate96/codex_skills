interface:
  display_name: "Google Chat Browser Fallback"
  short_description: "Fallback to approved Chrome control when gws is unavailable"
  default_prompt: "Use $google-chat-cdp with $chrome-cdp only after the standard chat-replies gws workflow is unavailable and the user explicitly approves browser fallback."

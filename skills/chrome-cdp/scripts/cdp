#!/usr/bin/env bash
set -euo pipefail

export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"
if [ ! -s "$NVM_DIR/nvm.sh" ]; then
  echo "nvm is required at $NVM_DIR/nvm.sh to run chrome-cdp with Node 22." >&2
  exit 1
fi

source "$NVM_DIR/nvm.sh"
nvm exec 22 -- node "$(dirname "$0")/cdp.mjs" "$@"

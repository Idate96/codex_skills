interface:
  display_name: "Ralpha Loop"
  short_description: "Implement-test-review iteration loop"
  default_prompt: "Use $ralpha-loop to run iterative implementation, verification, and persistent-review cycles until no findings remain."

interface:
  display_name: "Newton Sim ROS Startup"
  short_description: "Start Newton sim ROS stacks and checkpoint resume flows"
  default_prompt: "Use $newton-sim-ros-startup to start or restart the single-container Newton and Moleworks ROS stack with the requested bridge, maps, and checkpoint state."

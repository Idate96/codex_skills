interface:
  display_name: "Simple Research Code"
  short_description: "Maximize trustworthy research iteration speed"
  default_prompt: "Use $simple-research-code to simplify this research workflow around its real operating model. For a single-operator ROS public launch, start and own the complete application stack—including enabled sensor acquisition, perception, mapping, planning, coordination, control/execution, and workers—while naming one exact external robot-prerequisite procedure and readiness contract. Keep its tool model consistent, delete alternate owners and attach/reuse surfaces, avoid duplicate DDS absence guards, and validate exact artifacts or live safety state at the consuming boundary."

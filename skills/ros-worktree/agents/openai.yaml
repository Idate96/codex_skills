interface:
  display_name: "ROS Worktree"
  short_description: "Create an isolated ROS2 workspace via git worktrees/symlinks"
  default_prompt: "Use $ros-worktree to create an isolated ROS 2 worktree workspace and build or test it without disturbing the main workspace."
